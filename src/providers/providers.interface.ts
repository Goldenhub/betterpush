export interface VercelTokenData {
  access_token: string;
  token_type: string;
  installation_id: string;
  team_id: string;
  user_id: string;
  // id_token: string;
  // expires_in: number;
  // scope: string;
  // refresh_token: string;
}
